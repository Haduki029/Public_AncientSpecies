[HarmonyPatch(typeof(Pawn_HealthTracker))]
    internal class Pawn_HealthTracker_Patch
    {
        [HarmonyPrefix]
        [HarmonyPatch(nameof(Pawn_HealthTracker.HealthTick))]
        [HarmonyPatch(MethodType.Getter)] //ここでプロパティのGetterを指定してます。省略デフォ値はNormal(通常のメソッド)です。
        //[HarmonyPatch(new Type[] { typeof(Pawn) })] //引数指定が必要な場合はここで
        static bool HealthTick_Prefix()
        {
            //ほにゃほにゃ
            return true;
        }
    }